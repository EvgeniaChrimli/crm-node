services:
  db:
    image: postgres:16
    container_name: my_postgres
    restart: always
    environment:
      POSTGRES_USER: postgress
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: practice
    ports:
      - "5432:5432"
